{
  "name": "bedrock-agent",
  "version": "1.0.0",
  "main": "server/index.js",
  "bin": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "build": "pkg . --targets node18-win-x64 --output dist/Bedrock-CodeBuilder.exe --debug",
    "build:all": "pkg . --targets node18-win-x64,node18-macos-x64,node18-linux-x64 --out-path dist/ --debug",
    "build:test": "pkg . --targets node18-win-x64 --output dist/Bedrock-CodeBuilder-Debug.exe --debug --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "이 프로젝트는 마인크래프트 에이전트를 블록 코딩으로 제어할 수 있는 웹 기반 도구입니다.",
  "dependencies": {
    "child_process": "^1.0.2",
    "colors": "^1.4.0",
    "copy-paste": "^1.5.3",
    "express": "^4.21.2",
    "figlet": "^1.8.0",
    "fs-extra": "^11.3.0",
    "http": "^0.0.1-security",
    "socket.io": "^4.8.1",
    "uuid": "^11.0.5",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3",
    "pkg": "^5.8.1"
  },
  "pkg": {
    "assets": [
      "client/**/*",
      "public/**/*", 
      "blocks/**/*",
      "shared/**/*",
      "node_modules/figlet/fonts/**/*"
    ],
    "scripts": [
      "server/**/*.js",
      "shared/**/*.js"
    ],
    "targets": [
      "node18-win-x64"
    ],
    "outputPath": "dist"
  },
  "build": {
    "appId": "com.bedrock.agent",
    "productName": "BedrockAgent",
    "win": {
      "target": "nsis"
    }
  }
}
