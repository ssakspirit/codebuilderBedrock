# test-feature

새로운 기능이나 수정사항을 테스트합니다.

## 작업 순서

1. **테스트 환경 준비**
   ```bash
   npm start  # 또는 npm run dev
   ```

2. **브라우저 테스트**
   - Chrome/Edge 개발자 도구 열기 (F12)
   - Console 탭에서 로그 확인
   - Network 탭에서 Socket.IO 연결 확인

3. **Minecraft 연결**
   - Minecraft Bedrock Edition 실행
   - 월드 입장
   - 채팅창에서 연결 명령어 입력: `/connect localhost:3000`
   - 연결 성공 메시지 확인

4. **기능별 테스트**

   ### a) 블록 테스트
   - [ ] 블록이 Toolbox에 표시되는가?
   - [ ] 블록을 드래그할 수 있는가?
   - [ ] 블록이 올바르게 조립되는가?
   - [ ] 필드 값을 변경할 수 있는가?
   - [ ] 블록 색상과 모양이 올바른가?

   ### b) 명령어 등록 테스트
   - [ ] Hat 블록 생성 시 서버에 등록되는가?
   - [ ] 서버 로그에 등록 메시지가 표시되는가?
   - [ ] 중복 명령어 시 에러 메시지가 나오는가?
   - [ ] 블록 삭제 시 등록이 해제되는가?

   ### c) 명령어 실행 테스트
   - [ ] Minecraft에서 명령어 입력 시 블록이 실행되는가?
   - [ ] 서버 로그에 실행 메시지가 표시되는가?
   - [ ] Minecraft에서 의도한 동작이 수행되는가?
   - [ ] 연속 실행 시 문제가 없는가?

   ### d) 에이전트 명령어 테스트
   - [ ] 에이전트가 생성되었는가? (`/agent create`)
   - [ ] 이동 명령이 작동하는가?
   - [ ] 블록 설치/파괴가 작동하는가?
   - [ ] 인벤토리 명령이 작동하는가?

   ### e) 이벤트 테스트
   - [ ] 아이템 획득 시 이벤트가 발생하는가?
   - [ ] 블록 설치/파괴 시 이벤트가 발생하는가?
   - [ ] 올바른 블록이 실행되는가?

5. **엣지 케이스 테스트**
   - [ ] 빠른 연속 실행
   - [ ] 긴 명령어 시퀀스
   - [ ] 잘못된 입력값
   - [ ] 네트워크 연결 끊김/재연결
   - [ ] 여러 브라우저 동시 연결

6. **성능 테스트**
   - [ ] 명령어 실행 속도가 적절한가?
   - [ ] 메모리 누수가 없는가?
   - [ ] CPU 사용률이 정상인가?
   - [ ] 장시간 실행 시 문제가 없는가?

7. **로그 확인**

   ### 서버 로그
   ```
   ✅ 예상 로그:
   🎯 [명령어 수신] commandName
   ➡️ 마인크래프트로 전송: agent move forward

   ❌ 오류 로그 확인:
   - WebSocket 연결 오류
   - Socket.IO 이벤트 오류
   - 명령어 파싱 오류
   ```

   ### 클라이언트 로그
   ```
   ✅ 예상 로그:
   === 블록 실행 시작 ===
   실행할 코드: ...
   === 실행 완료 ===

   ❌ 오류 로그 확인:
   - eval() 오류
   - Socket 연결 오류
   - 블록 찾기 실패
   ```

8. **문제 발견 시**
   - 로그 메시지 복사
   - 재현 방법 기록
   - 예상 동작 vs 실제 동작 기록
   - `/fix-bug` 스킬 사용

## 빠른 테스트 체크리스트

### 새 블록 추가 시
```
□ 블록이 Toolbox에 보임
□ 블록 조립 가능
□ 코드 생성 확인 (console.log)
□ Socket.IO 이벤트 전송 확인
□ 서버 핸들러 동작 확인
□ Minecraft 명령 실행 확인
□ 로그 메시지 출력 확인
```

### 이벤트 추가 시
```
□ Hat 블록 등록 확인
□ 서버에 Map 등록 확인
□ Minecraft 이벤트 구독 확인
□ 이벤트 수신 확인
□ 블록 실행 확인
□ 중복 등록 방지 확인
```

### 버그 수정 시
```
□ 버그 재현 확인
□ 수정 후 버그 해결 확인
□ 관련 기능 정상 작동 확인
□ 새로운 버그 발생 안함
□ 성능 저하 없음
```

## 테스트 도구

### 브라우저 개발자 도구
- **Console**: 로그 및 오류 확인
- **Network**: Socket.IO 통신 확인
- **Application** > Storage: LocalStorage 확인

### Minecraft 명령어
```bash
/agent create                    # 에이전트 생성
/agent tp @s                     # 에이전트를 플레이어 위치로
/give @s diamond 64              # 아이템 지급 (테스트용)
/gamemode creative               # 크리에이티브 모드
/time set day                    # 낮으로 설정
```

## 참고 문서

- docs/ARCHITECTURE.md - 시스템 작동 원리
- docs/REFERENCES.md - API 레퍼런스
